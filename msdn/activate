export PIPENV_VENV_IN_PROJECT=1
hash -r
type python
python -m pip install -U --user pip pipx || return
hash -r
type pipx
pipx install pipenv || return
hash -r
type pipenv
pipenv sync || return
hash -r
pipenv shell || return
